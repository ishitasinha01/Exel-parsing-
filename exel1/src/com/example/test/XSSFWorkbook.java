package com.example.test;

import org.apache.poi.ss.usermodel.Workbook;

import java.io.FileInputStream;

public class XSSFWorkbook implements Workbook {
    public XSSFWorkbook(FileInputStream inputStream) {
    }
}
